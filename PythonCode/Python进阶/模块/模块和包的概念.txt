
为什么需要模块和包？
当工程较大，代码越来越多的时候，将代码放进一个文件中不利于维护。

模块和包的优点：
易于维护
同一个名字的变量相互之间没有影响。

自身模块名称：
文件名

引用其他模块：
import 模块名

使用其他模块：
模块名.方法名()

解决模块名称冲突：
放入不同的包中

完整的引用一个模块：
import 包名.模块名

使用这个模块：
包名.模块名.方法名()

包：文件夹
模块：py文件

如何区分包和普通目录：
包下面必须要有__init__.py
每一层都必须包含该文件